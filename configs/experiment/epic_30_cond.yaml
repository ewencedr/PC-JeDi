# @package _global_
defaults:
  - override /model: localepicjedi.yaml
trainer:
  check_val_every_n_epoch: 50
  enable_progress_bar: False

# loggers: null
# callbacks: null
datamodule:
  data_conf:
    log_squash_pt: False
    high_as_context: True
    n_jets: null
    one_hot_last: True
    rotate_csts: False

    jetnet_config:
      jet_type:
        # - g
        # - q
        - t
        # - w
        # - z
      data_dir: /srv/beegfs/scratch/groups/rodem/datasets/jetnet/
      num_particles: 30
      split_fraction: [0.6, 0.1, 0.3]
      particle_features:
        - etarel
        - phirel
        - ptrel
      jet_features:
        - pt
        # - eta
        - mass
        # - num_particles # Needed for the transformer to be dependant on multiplicity
        - type # Type must always be last if present
model:
  epic_jedi_config:
    latent: 10
  optimizer:
    lr: 0.0001
    weight_decay: 0.0001

project_name: epic_debug # Determines output directory path and wandb project
network_name: ${now:%Y-%m-%d}_${now:%H-%M-%S-%f}
